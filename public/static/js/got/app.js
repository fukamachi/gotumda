goog.provide('got.App');

/**
 * @param {String} baseUrl
 * @constructor
 */
got.App = function(baseUrl) {
  this.baseUrl = baseUrl;
};

got.App.prototype.allTasks = function(url) {
};

got.App.prototype.update = function(body) {
};
